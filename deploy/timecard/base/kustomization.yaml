apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: timecard

labels:
  - includeSelectors: true
    pairs:
      app: timecard

resources:
  - namespace.yaml
  - service.yaml
  - ingress.yaml
  - deploy.yaml

images:
  - name: docker.io/library/timecard
    newTag: 0.4.0
