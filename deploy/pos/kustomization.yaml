apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Default deployment for the pos app (version 0.4.0).
# Apply with: kubectl apply -k deploy/pos
# Override with overlays/0.5.0 or overlays/0.6.0.
resources:
  - overlays/0.4.0
