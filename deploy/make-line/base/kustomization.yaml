apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: make-line

labels:
  - includeSelectors: true
    pairs:
      app: make-line

resources:
  - namespace.yaml
  - service.yaml
  - ingress.yaml
  - deploy.yaml

images:
  - name: docker.io/library/make-line
    newTag: 0.4.0
