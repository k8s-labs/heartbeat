apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Default deployment for the make-line app (version 0.4.0).
# Apply with: kubectl apply -k deploy/make-line
# Override with overlays/0.5.0 or overlays/0.6.0.
resources:
  - overlays/0.4.0
