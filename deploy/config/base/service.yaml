apiVersion: v1
kind: Service
metadata:
  name: config
  namespace: config
  labels:
    app: config
spec:
  type: ClusterIP
  selector:
    app: config
  ports:
    - port: 8080
      targetPort: 8080
